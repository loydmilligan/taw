---
phase: 999.3
plan: "02"
subsystem: voice-service
tags: [voice, ttstt, subprocess, zod, config]
dependency_graph:
  requires: [999.3-00]
  provides: [VoiceService, voiceConfigSchema]
  affects: [src/services/config/schema.ts]
tech_stack:
  added: []
  patterns: [injectable-spawn-factory, discriminated-union-result, globalThis-timer]
key_files:
  created:
    - src/services/voice/index.ts
    - src/services/voice/config.ts
  modified:
    - src/services/voice/service.test.ts
    - src/services/config/schema.ts
decisions:
  - "createVoiceService factory pattern for injectable spawn — enables full mock coverage without module-level patching"
  - "globalThis.setTimeout used instead of setTimeout per PATTERNS.md project convention"
  - "voice block inserted between hister and allowedContextDirs in globalConfigSchema"
metrics:
  duration: "~10 min"
  completed: "2026-04-17"
  tasks: 3
  files: 4
---

# Phase 999.3 Plan 02: VoiceService Implementation Summary

VoiceService subprocess wrapper around ttstt CLI with injectable spawn factory, discriminated-union listen results keyed by exit codes, and Zod voice config block wired into globalConfigSchema.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 2.1 | Flesh out service.test.ts with mock-spawn assertions (RED) | a94433e | src/services/voice/service.test.ts |
| 2.2 | Implement config.ts + append voice block to globalConfigSchema | 2800af5 | src/services/voice/config.ts, src/services/config/schema.ts |
| 2.3 | Implement index.ts (VoiceService) — GREEN | 41ad5b5 | src/services/voice/index.ts |

## TDD Gate Compliance

- RED gate: commit a94433e (`test(999.3-02): flesh out service.test.ts...`) — 9 failing tests
- GREEN gate: commit 41ad5b5 (`feat(999.3-02): implement VoiceService...`) — 20 tests passing (9 service + 11 extract)

## Verification Results

- `pnpm test --run src/services/voice/` — 20 tests passed (service.test.ts + extract.test.ts)
- `grep -rE "exec(Sync)?\s*\(" src/services/voice/index.ts` — no matches (shell injection mitigated)
- `grep -q "SIGTERM" src/services/voice/index.ts` — present
- `grep -q "SIGKILL" src/services/voice/index.ts` — present
- `grep -q "globalThis.setTimeout" src/services/voice/index.ts` — present
- Config `globalConfigSchema.parse({}).voice.voiceName === 'alloy'` — confirmed at runtime

## Deviations from Plan

None — plan executed exactly as written.

## Threat Mitigations Applied

| Threat ID | Mitigation |
|-----------|-----------|
| T-999.3-01 | spawn() with args array only; test "never imports exec or execSync" enforces this at test time |
| T-999.3-02 | interruptSpeak sends SIGTERM, then SIGKILL after 500ms via globalThis.setTimeout; timer cleared on natural close |

## Known Stubs

None in files created by this plan.

## Self-Check: PASSED

- src/services/voice/index.ts — EXISTS
- src/services/voice/config.ts — EXISTS
- src/services/voice/service.test.ts — EXISTS (updated)
- src/services/config/schema.ts — EXISTS (updated, voice block present)
- Commits a94433e, 2800af5, 41ad5b5 — all present in git log
