---
phase: 999.3
plan: "01"
subsystem: voice
tags: [voice, tdd, extract, pure-function]
requires:
  - voice-test-scaffold
  - voice-fixture-transcripts
provides:
  - extractForVoice
affects: []
tech_stack:
  added: []
  patterns: [tdd-green, pure-function, regex-strip]
key_files:
  created:
    - src/services/voice/extract.ts
  modified:
    - src/services/voice/extract.test.ts
decisions:
  - Used first-line + trailing-question pattern for Brainstorm modes matching fixture expectations
  - Kept MODE_FOOTER_RE verbatim from RESEARCH.md Pattern 2 — matches "Options:" and "phase N" footer patterns
metrics:
  duration: ~10min
  completed: 2026-04-17
---

# Phase 999.3 Plan 01: extractForVoice Implementation Summary

Pure per-mode voice extractor implementing `extractForVoice(entry, mode): string` — strips mode footers, collapses transcript entries to spoken-length text using first-sentence/25-word cap for General and first-line+trailing-question for Brainstorm modes.

## Tasks Completed

| Task | Name | Commit |
| ---- | ---- | ------ |
| 1.1  | Flesh out extract.test.ts with 11 concrete assertions (RED) | a68ae89 |
| 1.2  | Implement extract.ts (GREEN) | f29b9c3 |

## Deviations from Plan

None — plan executed exactly as written.

Note: The test file already contained fully-fleshed assertions from the Wave 0 scaffolding context (the file on disk was the complete version, not the minimal stub described in the plan). Task 1.1 committed the final version.

## Verification

- `pnpm test --run src/services/voice/extract.test.ts` — 11/11 pass
- `pnpm lint src/services/voice/extract.ts` — zero errors in extract.ts
- No I/O imports in extract.ts (pure function, string transform only)

## Self-Check: PASSED

- `src/services/voice/extract.ts` exists with `export function extractForVoice`
- U+2500 and U+2501 regex constants present
- `extractBrainstorm` and `extractGeneral` helper functions present
- Commits f29b9c3, a68ae89 present in git log
- 11 tests pass, 0 fail
